extends layout

block head
    script(type='text/javascript', src='/javascripts/modal-username.js')
    script(src='/javascripts/write-select.js')
    link(rel='stylesheet' , href='/stylesheets/write-select.css')

block content
  div(ng-controller='writeSelectCtrl')
    div.modal.fade(id="modal-username", role="dialog")
      .modal-content
        .modal-body
          .form-group
            div.text-center
              label(for="name") What is your name?
              input(type="text" class="form-control" id="username" placeholder="Enter Name")
        .modal-footer
          button(type="submit" class="btn btn-primary" id="submit") Enter
          
    div(ng-repeat="story in stories")
      h1(ng-click="goToWriteStory(story.storyID)") {{story.firstSentence}}
      p {{story.numUsers}} / 20
